<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>


	<parent>
		<groupId>com.conztanz</groupId>
		<version>0.4-SNAPSHOT</version>
		<artifactId>framework</artifactId>
	</parent>
	<artifactId>framework-connect</artifactId>
	<packaging>pom</packaging>
	<properties>

		<!-- Local deployment dependencies -->
		<target.output.directory>target/dependency</target.output.directory>
		<servicemix.version>7.0.0.M2</servicemix.version>
		<servicemix.rootfolder>apache-servicemix-${servicemix.version}</servicemix.rootfolder>
		<servicemix.local.target.basedir>${target.output.directory}/${servicemix.rootfolder}</servicemix.local.target.basedir>
		<servicemix.local.target.deploydir>${servicemix.local.target.basedir}/deploy</servicemix.local.target.deploydir>
		<!-- <servicemix.install.filesystem.parentdir>C:</servicemix.install.filesystem.parentdir> -->
		<!-- <ftp.incoming.dir>${servicemix.install.filesystem.parentdir}/${servicemix.rootfolder}/ftp/incoming/edi</ftp.incoming.dir> -->



		<!-- <project.current.artifact.base>${product.connect.artifact.base}</project.current.artifact.base> -->
		<product.name>${product.connect.name}</product.name>
		<!-- <persistence.base.name>${product.company}${product.connect.name}</persistence.base.name> -->
		<persistence.ds.name>${product.company}${product.connect.name}DS</persistence.ds.name>
		<persistence.user.name>${product.company}${product.connect.name}User</persistence.user.name>
		<persistence.schema.name>${product.connect.name}Schema</persistence.schema.name>

		<bundle.vendor>Conztanz</bundle.vendor>
		<esbDeployableProject>false</esbDeployableProject>
	</properties>

    <modules>
        <module>../framework-connect-model</module>
        <module>../framework-connect-persistence-test</module>
        <module>../framework-connect-persistence</module>
        <module>../framework-connect-lifecycle-initialization</module>
        <module>../framework-connect-lifecycle-identification</module>
        <module>../framework-connect-lifecycle-continuity</module>
        <module>../framework-connect-lifecycle-services</module>
		<module>../framework-connect-lifecycle-processing</module>
        <!--TODO  : reorder-->
        <module>../framework-connect-lifecycle-orchestration</module>
        <module>../framework-connect-lifecycle-locking</module>
    </modules>
	<!--<profiles>-->
		<!--&lt;!&ndash; Modules defining ConztanZ Framework Connect &ndash;&gt;-->
		<!--<profile>-->
			<!--<id>modules</id>-->
			<!--<modules>-->
				<!--<module>../${framework.connect.artifact.base}-connection-factory</module>-->
				<!--<module>../${framework.connect.artifact.base}-model</module>-->
				<!--<module>../${framework.connect.artifact.base}-persistence</module>-->
				<!--<module>../${framework.connect.artifact.base}-lifecycle-initialization</module>-->
				<!--<module>../${framework.connect.artifact.base}-persistence-bundle</module>-->
				<!--<module>../${framework.connect.artifact.base}-spring-context</module>-->
				<!--<module>../${framework.connect.artifact.base}-spring-context-bundle</module>-->
			<!--</modules>-->
		<!--</profile>-->
	<!--</profiles>-->



</project>